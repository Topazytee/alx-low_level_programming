#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 */

int main(void)
{
	char v;

	char m;

	v = 'a';
	m = 'A';
	while
		(v <= 'z') {
			putchar(v);
			v++;
		}
	while
		(m <= 'Z') {
			putchar (m);
			m++;
		}
	putchar('\n');
	return (0);
}
