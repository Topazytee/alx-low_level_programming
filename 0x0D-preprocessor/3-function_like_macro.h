#ifndef MACROLIKE_H
#define MACROLIKE_H

/**
 * File: function_like_macro
 * @x: input value (int)
 * Desc: header file that computes the absolute value
 */
#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif
